tomorrow I will fix the
        stripe and coinvbse parts
        adnd the nest day I will style 
        and add videos and scroller
        we probably dont need a navigation 

at work 
    I want to do some more work with thw 
when using requrie ( using "" gives you the file path and '' gives you the dependency in json )

when doing a fetch request froom local host no s in hhtp